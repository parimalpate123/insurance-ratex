apiVersion: apps/v1
kind: Deployment
metadata:
  name: guidewire-mock
  namespace: insurratex
  labels:
    app: guidewire-mock
    component: mock
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guidewire-mock
  template:
    metadata:
      labels:
        app: guidewire-mock
        component: mock
    spec:
      containers:
      - name: guidewire-mock
        image: insurratex/guidewire-mock:latest
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3001"
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 20
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: guidewire-service
  namespace: insurratex
  labels:
    app: guidewire-mock
spec:
  type: ClusterIP
  selector:
    app: guidewire-mock
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
