apiVersion: apps/v1
kind: Deployment
metadata:
  name: rules-ui
  namespace: insurratex
  labels:
    app: rules-ui
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rules-ui
  template:
    metadata:
      labels:
        app: rules-ui
        component: frontend
    spec:
      containers:
      - name: rules-ui
        image: insurratex/rules-ui:latest
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: VITE_API_URL
          value: "http://orchestrator-service:3000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: rules-ui-service
  namespace: insurratex
  labels:
    app: rules-ui
spec:
  type: ClusterIP
  selector:
    app: rules-ui
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
